# Base Sepolia deployments

After deploying with `npm run deploy:sepolia` from `contracts/`, commit `base-sepolia.json` here.

Required env in `contracts/.env`:

- `BASE_SEPOLIA_RPC_URL`
- `DEPLOYER_PRIVATE_KEY`
- `PUBLISHER_SHARE_BPS=7000`
- `WORKER_SHARE_BPS=2500`
- `PROTOCOL_FEE_BPS=500`
